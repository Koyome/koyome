# Koyome.me 更新日志 — 2026-09-21（第十轮：素材清理 / 新图形 / 双地图 / 边框重设计）

> 本轮同时包含**上一轮未推送**的改动（人像抠图 + 星球重设计），一并上线。
> 提交 `3ccf06a`，线上已验证。

---

## 1. 素材清理

- 删除用户提供的两张设计素材及其衍生文件：`assets/deco/` 整个目录（compass-star / radiant-eye 的 jpg 与 webp 共 4 个文件）已移除，线上 404。
- 移除首页爱好区的罗盘星徽记（`.hh-emblem`）与 t2 条目的放射之眼冠饰（`.t2-emblem`）及其全部 HTML/JS/CSS 引用。
- 首页人像注释「——落墨於紙，無需畫框」**改为可编辑内容组件**：文字改存 `profile.json` 的 `figNote` / `figNoteZh` 字段（当前置空 = 元素隐藏）。站长在本机双击人像下方虚线占位即可输入，Enter 保存、Esc 取消；游客永远看不到占位。样式类 `.portrait-note.is-empty`。

## 2. 新增图形元素（Stone Island 式：几何、克制、标识感）

与首页星球同一套视觉语言（细线 + 虚线 + mono 小字 + 单点红 accent，CSS 变量驱动，日夜自适应）：

- **罗盘徽章 `.hh-compass`**（首页爱好区，接替被删的星图）：双圆环 + 度数刻度 + 四芒星罗盘（北极 accent 红）+ 每 60 秒一圈的扫秒针 + KOYOME 字标。reduced-motion 静止。
- **线条房屋组合 `.i1-houses`**（旅行条目页首）：建筑立面图——三座坡顶/平顶小屋 + 一栋网格公寓 + 一棵树，虚线标注线、尺寸界线、测量火花，mono 标注 `FIELD RECORD — 01` / `DWELLINGS · 住まい`。
- 星球图形保留不动（用户确认满意）。

## 3. 旅行地图重绘 + 济州岛新地图（可交互标注）

旧版潦草街网 + 虚线连线全部废弃，`entry.js renderMaps()` 重写：

- **东京图（560×400）**：参考真实东京地图数字化重绘——测量网格、主干道网、东京湾水域、隅田川河道、皇居/上野/新宿御苑绿地（虚线填充）、中央本线（虚线）、**山手线环 + 6 个站点**、池袋/上野/浅草/东京站/银座/品川等印刷体地名标注。照片图钉仍由描绘关键词驱动（东京塔/东大/你的名字取景地/新宿/涩谷），点击跳到对应照片。
- **济州岛图（560×380）**：同一制图标准——汉拿山盾形岛轮廓、牛岛、1132 环岛公路（虚线）、汉拿山 1,947M / 城山日出峰 / 山房山三角峰标、济州市/西归浦市、海域名标注。初始无图钉，等待站长标注。
- **交互标注**：站长（API 模式）点击地图任意空白处 → 浮出命名卡（中文名必填 + 英文选填，Enter 确认 / Esc 取消）→ 存入 `entry.mapPins.{tokyo,jeju}`（PUT /api/content，server.js 校验坐标钳制 0–560/0–400、每图 ≤60 枚）；点击自建图钉可确认删除。游客只看到只读图钉，无任何编辑入口。
- 按用户要求：**地标之间不画任何连线**。

## 4. 「私人剪辑」边框重设计

- 废弃胶片齿孔方案，改为**画廊展板**：发丝细框 + 左上角一小段红色角标 + 纸质标签牌（mono 小字）。极简、不抢影片本身。

## 部署与验证

- 提交 `3ccf06a`（10 文件，+668/−155），SSH 推送；线上抽查：`hh-compass`/`renderMaps`/`jejuSvg`/`renderPortraitNote`/`tm-island` 全部 200 且内容正确，被删素材 404。
- API 冒烟测试（临时实例）：mapPins 保存/钳制/清理、figNote 保存/恢复，零残留。
- jsdom 静态回归 22/24（2 个失败为留言板云读取的 jsdom 环境怪癖，与本轮无关，Supabase 实测 200）。

## ⚠️ 站长注意

**本机 server.js 需重启一次**（双击 `start-hidden.vbs` 或重新登录），新的地图标注 / 人像注释保存接口才会生效——前端改动刷新即可用，但「点地图存图钉」「双击存注释」依赖新 API 字段。
