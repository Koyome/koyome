# Koyome.me 更新日志 — 2026-09-21（第九轮大改版）

本次改版涵盖九项需求，全部完成并上线。以下为逐项说明。

---

## 1. 夜间模式（全站）

- 页面右上角菜单旁新增 **日/夜切换按钮**（太阳/月亮图标），所有页面可用。
- 夜间模式为纯黑纸底（`#121110`），正文近白（`#ebe9e5`，对比度约 15:1），次级文字与线条同步提亮，accent 红提亮为 `#d4604f` 保证暗底可读性。
- 选择记入 localStorage，全站记忆；`<head>` 内联预置脚本，刷新/换页**无白屏闪烁**。
- 照片类元素夜间自动解除 `mix-blend-mode: multiply`（否则暗底下图片会被埋没）。

## 2. 整体设计风格升级

参考提供的风格图（黑金罗盘、金色建筑线稿、轨道球、故障拼贴、十字星、切片海报）：

- 全站新增**图纸角标**：视口四角细线直角标记，如建筑图纸的图框（移动端隐藏）。
- 既有几何语言（角尺、虚线、mono 小标签、红 accent）全部保留并延伸到新组件，日夜两套配色均由 CSS 变量驱动。

## 3. 首页重新设计

- 保留全部主体元素（头像区、目录索引、爱好引导区、六芒星）。
- 新增**轨道雕塑**：hero 区右后方三组不同速度的旋转轨道环 + 红色虚线环 + 行星点，纯 CSS 自主运动，reduced-motion 自动静止。
- 个人图片区按参考图重设计：照片改为**钉在手绘街道地图上的相片**——背后是蜿蜒的街区线稿 SVG，照片带聚光灯暗角（vignette）、微倾斜悬停回正、红色十字标记与「你在这里」红点，下方新增一行手写感注释。

## 4. 爱好板块

- 首页爱好引导区图片改为**每日随机 4 张**：以当天日期为随机种子洗牌，同一天内刷新保持不变，跨天自动换新；下方新增「今日推薦」标注（双语）。
- 爱好页新增 **Galgame 分区**：沿用河流式交错布局，分区标题带 ✦ 标记，图片框为虚线外描边（与动漫区的拍立得框、角色区的实线双框区分开）。双语标题已入字典；站长模式下图文增删改、上传与其他分区完全一致；服务端字段裁剪原生兼容（≤4 分区）。

## 5. Q 版人物定位修复

- **Bug 根因**：装饰槽用百分比绝对定位在页面容器内，页面随新增内容变长，槽位就跟着下移。
- **修复**：改为 `position: fixed` 钉在视口上，增删内容位置永远不变。
- 插入点从 3 个增加到 **6 个**（左右页边各 3 个）；移动端最多显示 3 个并缩小贴角（延续「缩减而非移除」原则）；站长模式可点击空槽上传/替换/删除，逻辑不变。

## 6. 素材边框装饰（四个板块四种语言）

- **爱好页**：动漫区=拍立得+红色角尺（原有）；角色区=实线双框；Galgame 区=虚线外描边。
- **电台（t1）**：封面改**黑胶唱片**造型（圆形+中心孔），曲目卡两侧加均衡器刻度线。
- **旅行记录（i1）**：展品相片盘——留白衬底相框，标签改为**定位图钉**样式移至相片下缘。
- **私人剪辑（v1）**：**电影胶片**框——两侧纵向链齿孔（圆点阵列）。

## 7. 旅行记录地图

- i1 条目页素材区上方新增**足迹地图**（SVG）：手绘街道网 + 河流 + 红色虚线行进路线（自主流动），串起东京塔、东京大学、《你的名字》须贺神社、新宿、涩谷十字路口 5 个地点。
- 每个地点有几何地标小图标（塔/门/鸟居/楼群/斑马线）+ 双语名称；数据来自照片描绘，**给新照片写上含地名（如"东京塔"）的描绘会自动上地图**。
- 点击（或回车）任一地点，平滑滚动到对应照片。

## 8. Koyome 板块星空

- 「關於 Koyome」（t2）条目页新增**几何简化星空**画布背景：十字形与方形星点缓慢明灭、稀疏星座连线，**流星每 3–9 秒随机划落**（带渐隐彗尾）。
- 颜色读 CSS 变量，日夜模式自动适配；reduced-motion 时只画一帧静态星空；仅作用于 t2，不影响其他条目。

## 9. 性能优化

排查结论与处理：

- **首页头像 8.9MB PNG**（首屏必载）→ 生成 97KB 优化副本 `assets/opt/avatar_opt.jpg`（900px 渐进 JPEG），`profile.json` 引用已切换，**原图保留未动**。
- **旅行照片 8 张 PNG 共约 30MB** → 生成共约 3MB 的优化副本（1600px），`content.json` 中 i1 的 9 处引用已切换，**原图全部保留**。
- **私人剪辑 9 个视频（合计约 230MB）** 打开条目页即全部握手预取元数据 → `preload="none"`，点击播放才加载。
- **电台 10 首 MP3** 同上 → `preload="none"`。
- 图片统一补 `decoding="async"`，首页头像标记 `fetchpriority="high"`。

---

## 事故与恢复（如实记录）

改版中途，仓库 `.git` 目录被**非我方操作**送入了回收站（本 AI 的全部命令均不可能删除该目录；工作区文件完好）。处置：从回收站找回残片 → 验证对象库不完整 → 通过 SSH 从 GitHub 重新克隆 → 移植 `.git` 回项目根。远端 HEAD 与本地一致（`2e2c38c`），**零数据丢失**，残片已移出仓库留存于工作区目录。

## 校验

- 6 个 JS 改动文件 `node --check` 全过；jsdom 静态回归 24 项中 22 过，唯二失败（guestbook en 列表/静态发帖）在改动前基线上同样失败，为既有测试环境噪音，与本次改动无关。
- 本地服务冒烟：全部页面/API 200，galgame 分区、6 装饰槽、优化头像、主题预置脚本、轨道装饰、地图画像均在线。

## 红线复核

- 未删除/覆盖 `docs/assets/` 任何已存在文件（仅新增 `assets/opt/` 衍生图）。
- 未重置任何 data json：hobbies.json 仅追加 galgame 空分区与 d4–d6 空槽；content/profile 仅切换图片引用，文字内容零改动。
- 全部路径保持相对路径；新增 UI 文案 en/zh 双字典齐全。
